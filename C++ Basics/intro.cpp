#include <iostream>

int main () {
    std::cout << "hello World";
    return 0;
}

/* In C++, a namespace is a mechanism that allows you to group related identifiers 
(such as classes, functions, and variables) under a common name. 
This helps to avoid naming conflicts between identifiers that may be defined in different parts 
of your code or in external libraries. */

